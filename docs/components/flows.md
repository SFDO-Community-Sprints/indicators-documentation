---
layout: default
parent: Components
title: Flow Components
nav_order: 2
has_children: false
---

## Flow Components

Salesforce Indicators has two Flow components - see [Indicators in Flow](../setup-salesforce-indicators/indicator-flow.md) for full setup details:

* **Indicator Bundle** - the full Bundle, displayed exactly as it would be on a Record Page.
* **Indicator Item** - a single, completely configurable Indicator for when you just need one Indicator on a Flow Screen.

{: .info-title}
>In Progress
>
>This page needs its own screen shots and examples for each Flow component - see [Indicators in Flow](../setup-salesforce-indicators/indicator-flow.md) in the meantime.

{: .note-title}
>Claude Notes
>
>- This page and setup-salesforce-indicators/indicator-flow.md cover the same two components (Bundle in Flow, single Item in Flow) - right now this one is a thin summary and the other has the real content, which is a reasonable split (Components = what exists, Setup = how to configure it), but it's not stated anywhere that's the intended split, so a future editor might just merge them by accident. Worth a one-line note on both pages clarifying the relationship.
